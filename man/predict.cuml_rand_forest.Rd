% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/rand_forest.R
\name{predict.cuml_rand_forest}
\alias{predict.cuml_rand_forest}
\title{Predict using a random forest model.}
\usage{
\method{predict}{cuml_rand_forest}(
  model,
  x,
  cuml_log_level = c("off", "critical", "error", "warn", "info", "debug", "trace")
)
}
\arguments{
\item{model}{A random forest model object.}

\item{x}{The input matrix or dataframe. Each data point should be a row and
should consist of numeric values only.}

\item{cuml_log_level}{Log level within cuML library functions. Must be one of
{"off", "critical", "error", "warn", "info", "debug", "trace"}.
Default: off.}
}
\description{
Perform classification or regression tasks using a trained random forest
model.
}
\examples{
library(cuml4r)

model <- cuml_rand_forest(
  iris,
  formula = Species ~ .,
  trees = 100
)

predictions <- predict(model, iris)

print(predictions)

}
