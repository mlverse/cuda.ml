#' @param model A model object.
#' @param x The dataset to be transformed.
#' @param ... Additional model-specific parameters (if any).
#'
#' @return \code{cuda_ml_transform()} returns coordinates in the learned
#'   representation. \code{cuda_ml_inverse_transform()} returns reconstructed
#'   predictors in the original feature space.
